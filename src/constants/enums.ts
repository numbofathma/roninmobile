export enum AlertTypes {
  INFO = 'info',
  DANGER = 'danger',
  SUCCESS = 'success',
  WARNING = 'warning',
  DEFAULT = 'default',
}

export enum SvgIcons {
  send = 'send',
  follow = 'follow',
  alert = 'alert',
  left = 'left',
  right = 'right',
  down = 'down',
  back = 'back',
}

export enum DomainCheckTypes {
  MX = 'mx',
  LOOKUP = 'lookup',
  NS = 'ns',
}

export enum Position {
  TL = 'top-left',
  TR = 'top-right',
  BL = 'bottom-left',
  BR = 'bottom-right',
}

export enum Breakpoints {
  xs = 'xs',
  sm = 'sm',
  md = 'md',
  lg = 'lg',
  xl = 'xl',
  '2xl' = '2xl',
  '3xl' = '3xl',
  '4xl' = '4xl',
}

export const enum AppPlatforms {
  IOS = 'ios',
  ANDROID = 'android',
  WEB = 'web',
}

export const enum Orientation {
  LANDSCAPE = 'landscape',
  PORTRAIT = 'portrait',
}
